---
- name: Install Docker on managed nodes
  hosts: managed_nodes
  become: true

  vars:
    docker_edition: ce
    docker_install_compose_plugin: true
    docker_users:
      - ubuntu

  roles:
    - role: geerlingguy.docker
