---
- name: Install common packages
  ansible.builtin.apt:
    name:
      - ca-certificates
      - curl
      - unzip
      - python3
      - python3-pip
      - awscli
    update_cache: true
    state: present
